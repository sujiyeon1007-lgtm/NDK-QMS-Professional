const fs=require("fs");const f="c:/Users/user1/Desktop/NDK-QMS-Professional/src/pages/Settings/MasterDataRegisterModal.jsx";let s=fs.readFileSync(f,"utf8");const o="    [open]\r\n  );\r\n\r\n  const updateField = (key, value) => {";const n="    [open]\r\n  );\r\n\r\n  const processDetailOptions = useMemo(\r\n    () => getProductProcessDetailOptions(form.processCategory),\r\n    [form.processCategory]\r\n  );\r\n\r\n  const updateField = (key, value) => {";if(!s.includes(o))throw 1;fs.writeFileSync(f,s.replace(o,n));console.log('ok3');
