const fs=require("fs");const f="c:/Users/user1/Desktop/NDK-QMS-Professional/src/pages/Settings/MasterDataRegisterModal.jsx";let s=fs.readFileSync(f,"utf8");const o="    if (isCompanyScreen && !payload.abbreviationManual && !payload.abbreviationLocked) {\r\n      payload.abbreviation = autoAbbreviation || payload.abbreviation;\r\n      payload.code = payload.code || payload.abbreviation;\r\n    }\r\n\r\n    const validation = validateMasterRow(";const n=o.replace("const validation","    if (isProductScreen && mode === \"add\" && payload.company && !payload.code) {\r\n      payload.code = generateProductManagementCode(payload.company);\r\n    }\r\n\r\n    const validation");if(!s.includes(o))throw 1;fs.writeFileSync(f,s.replace(o,n));console.log('ok5');
