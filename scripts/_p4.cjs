const fs=require("fs");const f="c:/Users/user1/Desktop/NDK-QMS-Professional/src/pages/Settings/MasterDataRegisterModal.jsx";let s=fs.readFileSync(f,"utf8");const o="      if (isCompanyScreen && key === \"abbreviation\") {\r\n        next.abbreviationManual = true;\r\n        next.abbreviationLocked = mode === \"edit\";\r\n        next.code = String(value ?? \"\").trim().toUpperCase() || prev.code;\r\n      }\r\n      return next;";const n=o.replace("return next;","      if (isProductScreen && key === \"company\" && mode === \"add\") {\r\n        next.code = value?.trim() ? generateProductManagementCode(value) : \"\";\r\n      }\r\n      if (isProductScreen && key === \"processCategory\") {\r\n        const details = getProductProcessDetailOptions(value);\r\n        next.processDetail = details.includes(prev.processDetail) ? prev.processDetail : details[0] ?? \"\";\r\n      }\r\n      return next;");if(!s.includes(o))throw 1;fs.writeFileSync(f,s.replace(o,n));console.log('ok4');
