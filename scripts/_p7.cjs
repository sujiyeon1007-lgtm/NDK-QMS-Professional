const fs=require("fs");const f="c:/Users/user1/Desktop/NDK-QMS-Professional/src/pages/Settings/MasterDataRegisterModal.jsx";let s=fs.readFileSync(f,"utf8");const o="    if (mode === \"add\") {\r\n      return {\r\n        readOnly: true,\r\n        helperText: \"업체명 기준 자동 생성 · 사용자 직접 입력 불가\",\r\n      };\r\n    }\r\n\r\n    if (form.abbreviationLocked) {";if(!s.includes(o)) {console.log('abbrev already patched?'); process.exit(0);} const n="    if (form.abbreviationLocked) {";fs.writeFileSync(f,s.replace(o,n));console.log('ok7');
