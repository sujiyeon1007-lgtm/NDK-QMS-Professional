const fs=require("fs");const f="c:/Users/user1/Desktop/NDK-QMS-Professional/src/pages/Settings/MasterDataRegisterModal.jsx";let s=fs.readFileSync(f,"utf8");const o="  form.abbreviationManual = Boolean(initialRow.abbreviationManual);\r\n  form.abbreviationLocked = Boolean(initialRow.abbreviationLocked);";const n="  if (screen?.categoryKey === \"products\") {\r\n    form.processCategory =\r\n      initialRow.processCategory ?? inferProcessCategoryFromDetail(initialRow.processDetail, initialRow.process);\r\n    form.processDetail = initialRow.processDetail ?? initialRow.process ?? \"\";\r\n  }\r\n  form.abbreviationManual = Boolean(initialRow.abbreviationManual);\r\n  form.abbreviationLocked = Boolean(initialRow.abbreviationLocked);";if(!s.includes(o))throw 1;fs.writeFileSync(f,s.replace(o,n));console.log('ok6');
